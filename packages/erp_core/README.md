# THQ ERP Core

Shared domain/runtime primitives used by THQ Client and THQ POS.

V4.7 establishes the package as the common contract layer rather than the original sample package. Current shared primitives include the release/database contract, fixed-minor-unit money value and standardized ERP failure shape. Further duplicated business models/services can move here incrementally without merging the distinct Client and POS user interfaces.
